var defaults = {
  aspectRatio: 0,
  cropperMode: 'none',
  dragMode: 'crop',
  movable: true,
  preview: undefined,
  responsive: false,
  toolbar: true,
  zoomable: true
}

module.exports = defaults
